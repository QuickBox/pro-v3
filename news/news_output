#!/bin/bash
declare -g title text
title="An update is available!"
text="## 3.0.0-beta.2582 (2022-07-12)

### enhancement (1 change)

- enh(calibre): stage db.template for deb10 as calibre-server has deprecated libc6 < 2.31

### fix (3 changes)

- fix(rtorrent): removed deprecated versions "0.9.4" and "feature-bind" from build
- fix(rtorrent): no longer removing flood on uninstall as flood supports multiple clients
- fix(flood): correction to install/reinstall header message"
