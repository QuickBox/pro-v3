#!/bin/bash
declare -g title text
title="An update is available!"
text="## 3.0.0-beta.2552 (2022-07-09)

### feature (2 changes)

- feat(UI): adjustments to appinfo array to show sickgear API on Dashboard
- feat(lazylibrarian): adjustments to build and appinfo array to show API on Dashboard

### fix (5 changes)

- fix(UI): correction to value searching per lazylibrarian api dashboard display
- fix(UI): set new display field values for lazylibrarian api key
- fix(unpackerr): ensure full, non-interactive removal on uninstall
- fix(qbittorrent): permission setting on data directory only if it exist
- fix(php): force set cgi.fix_pathinfo in the event it defaults to 0

### enhancement (1 change)

- enh(php): better backup and subsequent cleanup of old backed files"
