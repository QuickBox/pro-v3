#!/bin/bash
declare -g title text
title="An update is available!"
text="## 3.0.0.2624 (2022-07-16)

### fix (4 changes)

- fix(fail2ban): corrections to fail2ban config build and config triggering
- fix(autocomplete): remove "-u" completion from fail2ban tabbing
- fix(fail2ban): remove user check from build as no username is required - installs under sysop
- fix(translation): better translation to CLEAR on Danish "nulstil""
